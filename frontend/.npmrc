# frontend/.npmrc
# Evita scripts postinstall arbitrarios
ignore-scripts=true

# Usa lockfile estricto
frozen-lockfile=true

# Auditoría automática
audit=true

# Registro oficial (evita mirrors no oficiales)
registry=https://registry.npmjs.org/

# Verificación de integridad
prefer-integrity=true